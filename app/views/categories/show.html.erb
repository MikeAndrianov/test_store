<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Additional fields:</strong>
  <%= @category.additional_fields.map{|key, value| key }.join(', ') %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
